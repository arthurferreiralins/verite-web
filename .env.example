# Injetadas automaticamente pela Vercel ao adicionar as integrações
# Storage -> Postgres (Neon) e Storage -> Blob no dashboard do projeto.
# Dependendo da integração o nome pode vir como DATABASE_URL ou POSTGRES_URL
# — api/_lib/db.js aceita qualquer um dos dois.
# DATABASE_URL=
# BLOB_READ_WRITE_TOKEN=

# Definidas manualmente em Settings -> Environment Variables.
# Nunca commitar os valores reais.
ADMIN_EMAIL=
ADMIN_PASSWORD_HASH=
SESSION_SECRET=
